# Adana Open Sahada — m.adanaopen.com

Sahadaki herkes için cep uygulaması. Turnuva sitesi (`adanaopen.com`) ayrı projedir: `adana-open`.

```bash
npm install
npm run dev
```

Yerelde http://localhost:3001

Hostinger: Node.js web app + GitHub, domain `m.adanaopen.com`.
